#include <stdio.h>
int sort(int a)
{
    int i, arr[100], temp;
    for (i = 0; i < a; i++)
    {
        scanf("%d", &arr[i]);
    }

    for (i = 0; i < a; i++)
    {
        if (arr[i])
    }

    for (i = 0; i < a; i++)
    {
        printf("%d ", arr[i]);
    }
}
int main()
{
    int n;
    scanf("%d", &n);
    sort(n);
    return 0;
}